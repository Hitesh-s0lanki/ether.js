{
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.8.1",
    "fs-extra": "^11.1.1",
    "solc": "^0.8.0"
  },
  "scripts": {
    "compile": "solcjs --bin --abi --include-path node_modules/ --bas-path . -o . SimpleStorage.sol"
  }
}
